name: CI

on:
  workflow_dispatch:
    inputs:
      update_visual_snapshots:
        description: Generate visual baselines as an artifact
        type: boolean
        default: false
  pull_request:
    branches: [testing]
  push:
    branches: [testing]

permissions:
  contents: read

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Scan repository for secrets
        uses: docker://ghcr.io/gitleaks/gitleaks:v8.30.1
        with:
          args: detect --source=/github/workspace --redact --verbose --no-banner

  validate:
    runs-on: ubuntu-latest
    env:
      # Marcadores para CI (la firma anti-spam y el hash de IP exigen >=16 caracteres).
      # No son secretos reales; el e2e solo necesita que existan.
      LEAD_FORM_SECRET: ci-placeholder-lead-form-secret
      LEAD_IP_SALT: ci-placeholder-lead-ip-salt
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Restore Turborepo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm lint:css
      - run: pnpm check:tokens
      - run: pnpm check:assets
      - run: pnpm typecheck
      - run: pnpm test:unit
      - name: Levantar Supabase local (para el e2e de captura de leads)
        run: pnpm exec supabase start -x vector
      - name: Exportar credenciales de Supabase al entorno
        run: |
          eval "$(pnpm exec supabase status -o env)"
          {
            echo "SUPABASE_URL=$API_URL"
            echo "SUPABASE_SERVICE_ROLE_KEY=$SERVICE_ROLE_KEY"
            echo "NEXT_PUBLIC_SUPABASE_URL=$API_URL"
            echo "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=$ANON_KEY"
          } >> "$GITHUB_ENV"
      - name: Install Playwright browser
        run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e
      - name: Detener Supabase
        if: always()
        run: pnpm exec supabase stop --no-backup || true
      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-evidence
          path: test-results/
          if-no-files-found: ignore
      - run: pnpm build
      - name: Dependency audit
        # Bloquea solo advisories critical (RCE-class). Los high de tooling
        # transitivo (DoS) se monitorean aparte para no romper CI con cada CVE nuevo.
        run: pnpm audit --audit-level critical

  visual-baselines:
    if: github.event_name == 'workflow_dispatch' && inputs.update_visual_snapshots
    runs-on: ubuntu-latest
    env:
      # Igual que en `validate`: permite renderizar la página de campaña para capturar.
      LEAD_FORM_SECRET: ci-placeholder-lead-form-secret
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm exec playwright test --project=visual --update-snapshots
      - uses: actions/upload-artifact@v4
        with:
          name: visual-baselines-linux
          path: tests/e2e/visual/**/*-snapshots/
          if-no-files-found: warn
