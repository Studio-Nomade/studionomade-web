# Paridad con los kits originales

La revisión compara jerarquía, composición y ritmo, no píxeles: el frontend usa San Diego donde el kit
de navegador usaba Archivo. Las capturas finales se realizan a 1440 × 900 con `deviceScaleFactor: 1`.

Estado del orden continuo:

- D2: galería de los 17 componentes de campaña capturada en evidencia local.
- D6: añadirá la comparación de `ui_kits/campaign/` contra `/inmobiliarias`.
- D7: añadirá `ui_kits/website/` contra Branding y Architecture.
- D8: publicará la galería permanente `/lab/ds/[component]`.

Las baselines Linux se generan mediante el workflow manual de CI y se revisan antes de commitearlas.
