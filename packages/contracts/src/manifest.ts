export interface ContractManifest {
  repository: string;
  tag: string;
  schema_version: string;
}

export const exampleManifest: ContractManifest = {
  repository: "pendiente",
  tag: "pendiente",
  schema_version: "0.0.0"
};
